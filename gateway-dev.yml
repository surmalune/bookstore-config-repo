spring:
    cloud:
        config:
            enabled: true
        
        gateway:
            discovery:
                locator:
                    enabled: true
                    lower-case-service-id: true
    
    # security:
    #     oauth2:
    #       resourceserver:
    #         jwt:
    #           issuer-uri: http://keycloak:8080/realms/auth

server:
  port: 8000
